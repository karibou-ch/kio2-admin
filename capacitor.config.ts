import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'kio2-admin-a11',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
